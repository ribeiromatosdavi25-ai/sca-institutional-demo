import { InstitutionalInfoPage } from '../../components/access/InstitutionalInfoPage';

export default function SystemInformationPage() {
  return (
    <InstitutionalInfoPage
      section="System"
      title="System Information"
      description="Platform identity, service boundaries, release metadata, and operational ownership of the SCA institutional environment."
    />
  );
}
