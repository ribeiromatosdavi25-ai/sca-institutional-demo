import { InstitutionalInfoPage } from '../../components/access/InstitutionalInfoPage';

export default function SecurityPolicyPage() {
  return (
    <InstitutionalInfoPage
      section="Policy"
      title="Security Policy"
      description="Baseline security controls, incident response standards, and access governance for SCA institutional operations."
    />
  );
}
